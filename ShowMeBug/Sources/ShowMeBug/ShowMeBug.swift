// 可以引⼊的库和版本相关请参考 “环境说明”
// 请勿更改 `ShowMeBug` 类名以防执⾏失败

class ShowMeBug {
    /**
     * - Parameters:
     *   - a: 整型
     *   - b: 整型
     * - Returns: 整型
     */
    func solution(_ a: Int, _ b: Int) -> Int {
        // 在这⾥写代码
        
        return a + b
    }
}